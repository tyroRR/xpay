import index from '../../api/index';
import * as types from '../mutation-types'

const state = {
  req: []
}

// getters
const getters = {
  reqData: state => state.res
}

// actions
const actions = {

}

// mutations
const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}
